{
  "name": "alarm",
  "version": "1.0.0",
  "description": "Alarm — Android app built with Capacitor",
  "private": true,
  "scripts": {
    "build": "echo 'No build step — static HTML app'",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.9",
    "@capacitor/core": "^7.6.9",
    "@capacitor/local-notifications": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "7.6.9"
  }
}
