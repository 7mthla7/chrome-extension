{
  "name": "alarm",
  "version": "1.0.0",
  "description": "Alarm — iOS app built with Capacitor",
  "private": true,
  "scripts": {
    "build": "echo 'No build step — static HTML app'",
    "cap:add": "npx cap add ios",
    "cap:sync": "npx cap sync ios",
    "cap:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/local-notifications": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
